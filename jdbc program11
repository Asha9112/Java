package com.tap.app;

import java.sql.CallableStatement;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.sql.Types;
import java.util.Scanner;
// count the number of employees salary is greater than specific salary Stored procedure:

public class program11 {
	static String url="jdbc:mysql://localhost:3306/jdbcClasses";
	static String username="root";
	static String password="Asha";
	private static Connection con;

	public static void main(String[] args) {
		try {
			//Load the driver
		Class.forName("com.mysql.cj.jdbc.Driver");
		// Establish connection to database
		con = DriverManager.getConnection(url, username, password);
		// create statement(prepareCall)
		CallableStatement call=con.prepareCall("{call count_emp_salary(?)}");
		// Set the parameters 
		System.out.println("Enter the salary");
		Scanner scan= new Scanner(System.in);
		int sal=scan.nextInt();
		call.setInt(1,sal);
		call.registerOutParameter(1, Types.INTEGER);
		// execute the query
		call.execute();
		//retrieve the output parameter value
		int res=call.getInt(1);
		System.out.println(res);

	}
		catch(ClassNotFoundException e) {
			e.printStackTrace();
		}
		catch(SQLException e) {
			e.printStackTrace();
		}

}
}
